# Blueprint Platform — Project Context for Claude Code

## What this project is

Blueprint is an Angular **project builder**, not just a component library. A developer runs a CLI (`create-blueprint-app`, not yet built) or `npx create-nx-workspace --preset=@blueprint-platform/foundation`, answers a few prompts, and gets a fully wired, production-ready Angular project: zoneless, signal-driven, themed, with a headless-first component system already in place.

There are **two separate repositories** involved:
- **`blueprint-platform`** (this repo) — an Nx workspace hosting four independently-versioned, independently-published npm packages ("pillars"). This is where generator logic lives.
- **`blueprint-reference`** (sibling repo, plain Angular CLI, no Nx) — a hand-developed sandbox app where components/theme are built and visually verified *before* being manually synced into this repo's generator templates. Never edit `blueprint-platform` generator template content without first verifying the equivalent in `blueprint-reference`, unless the content is generator-only (has no reference-app equivalent, e.g. `app.routes.ts.template`).

## The four pillars (all in this one Nx workspace, separately versioned)

| Pillar | Package | Role |
|---|---|---|
| 1. Foundation | `@blueprint-platform/foundation` | The `preset` generator — scaffolds the whole app by default. Theme tokens, config-provider, base spartan-derived UI (button, utils), routing skeleton, manifest. |
| 2. Components | `@blueprint-platform/components` | Hosts **all ~64 spartan-derived UI component sources** + the single `ui` generator that copies them (and their deps) into a generated project on demand. `foundation` depends on this and delegates the starter-set + layout-set copies to it. Bigger composite pieces (data-table, audit-log) — not yet built. |
| 3. Modules | `@blueprint-platform/modules` | On-demand generators for business domains. **`auth`, `rbac`, `user-management` built** (see Current status) — the latter two are fully decoupled from `auth` and from each other in every direction; other business domains not yet built. |
| 4. Templates | `@blueprint-platform/templates` | Composite generators bundling components+modules+layout into presets like admin-dashboard — **not yet built**. |

Plus **`@blueprint-platform/generator-kit`** — not a pillar (nothing a generated project ever depends on), a plain library of shared generator-authoring helpers (`appendProvider`, `wrapRoutesUnderLayout`, `registerUiPaths`, `appendToManifest`) that `foundation`/`components`/`modules`/`templates` depend on **in their own `package.json`** to write *their* generators. **Hard rule: `generator-kit` must never appear in a generated project's `package.json`** — it's a dependency of the generator packages' own code, not of anything they produce. `preset.ts` writing to a generated project's `package.json` and `packages/foundation/package.json` itself are two completely different files; conflating them here is the easiest mistake to make.

**Versioning:** each pillar has its own independent semver line (`"projectsRelationship": "independent"` in `nx.json`'s release config — this must never be lost or accidentally reset to lockstep). `packages/foundation/compatibility.json` bundles a snapshot of which versions of the other three pillars are known-compatible with the current foundation version — this file travels with the published package, no runtime network fetch. A generated project's `.blueprint/manifest.json` records exactly which versions it was generated from.

## Current status (update this section as work progresses)

- ✅ Foundation preset (`preset.ts`) generates successfully end-to-end: app scaffolds (`@nx/angular` `applicationGenerator`, `rootProject: true`, scss, no routing), base npm deps added to the generated `package.json` (`@angular/cdk` + Tailwind v4 / `@tailwindcss/postcss` / `postcss` dev deps; `@blueprint-platform/components` as a devDependency; **all other UI npm deps now come from the `ui` generator** — see the starter-set bullet), `.postcssrc.json` written, SCSS token system + `tailwind-theme.css` copied to `src/styles/`, config provider (`provide-blueprint.ts` + `template-config.ts`) generated into `src/app/core/config/`, the **starter UI set** copied to `src/app/shared/ui/` (with `@blueprint-platform/ui/<name>` tsconfig paths) by delegating to `@blueprint-platform/components:ui`, `app.routes.ts` skeleton written, `app.config.ts` AST-patched (`provideZonelessChangeDetection`, `provideRouter(routes)`, `...provideBlueprint(...)`), `project.json` `styles` array set (`theme.scss` then `tailwind-theme.css`), `shared/components` / `features` / `layout` / `global` seeded with `.gitkeep`, and `.blueprint/manifest.json` written (channel from `BLUEPRINT_CHANNEL` env, default `stable`; versions copied from `compatibility.json`; `components` = the installed UI-component list).
- ✅ Preset generator options: `name` (required), `palette` (default `"default"`), `rtl` (default `false`), `labelPosition` (`"floating" | "inline" | "top"`, default `"floating"`), `layout` (`"sidebar-shell" | "floating-shell" | "inset-shell" | "topbar-shell" | "none"`, default `"none"`), `showThemeSwitcher` (boolean, default `true`), `showLanguageSwitcher` (boolean, default `true`), `apiUrl` (string, default `""` — **origin only**, see the environments bullet), `appVersion` (string, default `"0.1.0"`). `schema.d.ts` was realigned with `schema.json` (previously had a duplicate `labelPosition` key + wrong union) — both are now in sync; `schema.json` stays the source of truth.
- ✅ ngx-translate v18 multi-language (foundation `0.1.25`, components `0.1.3`). **Three-tier split gating**, not two — a bug found via `foundation:layout` regression testing (below) forced a redesign of the original two-tier split: layout shells inject `LanguageService` **unconditionally** for their own RTL/LTR side computation (same tier as `ThemeService`), so it can't require `@ngx-translate/core` to exist. `LanguageService` now ships as **two variants** in `files/language/`: `language.service.ts` (BASE — direction-tracking + the CDK `Directionality` shim, no ngx-translate import) and `language.service.translate.ts` (adds `inject(TranslateService)` + `.addLangs`/`.setFallbackLang`/`.use()`). Both implement the identical public shape (`available`, `current`, `dir`, `value`/`change`, `setLanguage`), so nothing downstream (shells, `language-switcher`) cares which one is present.
  - **Tier 1 — base `LanguageService`** exists whenever `layout !== 'none' OR showLanguageSwitcher`. `preset.ts` computes `hasLanguageService` for this; `patch-app-config.ts`'s `{ provide: Directionality, useExisting: LanguageService }` is gated on it (param renamed from `showLanguageSwitcher`). The `layout` generator ensures it directly — mirrors the existing `theme.service.ts` "generate if `core/theme` missing" pattern, using `DEFAULT_LANGUAGES = ['en','ar']` since it has no `languages` option of its own.
  - **Tier 2 — the ngx-translate engine** (deps, `src/assets/i18n/*.json`, `provideTranslateService`/`provideTranslateHttpLoader`, and writing the **translate-augmented** service variant over the base one) is still gated on **`showLanguageSwitcher` alone**, independent of `layout` — a `layout: none` project can opt in and get full infra with no pre-built UI.
  - **Tier 3 — the `language-switcher` dropdown component** (N-language `hlm-dropdown-menu` over `LanguageService.available`) needs **both** a layout and the tier-2 engine — checked via `tree.exists('src/assets/i18n')`, **not** `core/language/` (which now exists for tier 1 alone too). `user-menu.ts` hard-imports it via a *relative* sibling path, so `components:ui`'s dependency-closure resolver pulls it in regardless of the requested component list whenever `user-menu` is requested — `layout.ts` now explicitly `tree.delete`s the dangling file (it would actually compile fine against the base service; it's removed because the developer opted out of the switcher UI, not because of a compile error) and un-records it from the manifest when tier 3 shouldn't apply, in addition to `patch-user-menu.ts` stripping the wiring.
  - `renderLanguageRegistryModule` / `renderAvailableLanguagesModule` (rendering `core/language/language-registry.ts` + `available-languages.ts` from the canonical `lib/language-registry.ts`) moved to `generators/shared/render-language-files.ts` so both `preset.ts` and `layout.ts` can call them.
  - **`lib/language-registry.ts`** is the compile-time canonical map of `code → { label, dir }` (8 codes: en, ar, fr, es, de, he, fa, ur); `preset.ts` validates `--languages` against it (**throws** listing valid codes on an unknown one). **Direction always follows the registry** — never an `if (code === 'ar')` anywhere. i18n JSON: real `en.json`/`ar.json` authored directly in `files/i18n/` (no `blueprint-reference` round-trip this round); any other requested code gets English content + a `_note` placeholder key. `languages` recorded in `.blueprint/manifest.json`. **Verified v18 API names** (against `@ngx-translate/core@18` / `@ngx-translate/http-loader@18`): `provideTranslateService`, `provideTranslateHttpLoader` (returns `Provider[]`, assignable to `RootTranslateServiceConfig.loader`), `TranslateService.setFallbackLang` / `.addLangs` / `.use` (no `setDefaultLang`), `TranslatePipe`. New schema option `languages` (`string[]`, default `["en","ar"]` — `preset.ts` also `?? ['en','ar']` defensively).
- ✅ Theme / language services + switchers (cross-repo sync, `task-sync-unified.md`; **language half superseded by the ngx-translate bullet above** — `language.service.ts` now follows `showLanguageSwitcher`, not `layout`, and was rewritten): `files/theme/theme.service.ts` and `files/language/language.service.ts` are top-level source folders (distinct from `files/styles/`, which is SCSS/Tailwind); each gets its own `**/*.ts` assets glob in `foundation/package.json`. `theme.service.ts` → `src/app/core/theme/` **only when `layout !== 'none'`**; `language.service.ts` → `src/app/core/language/` **when `showLanguageSwitcher`** (any layout). Destination convention: services under `core/`, not `shared/`. Three flat (no `src/index.ts`) components — `user-menu/`, `theme-switcher/`, `language-switcher/` — now live with the rest of the kit under `packages/components/…/files/shared/ui/` and are added **only alongside a layout** (the `ui` generator call in `preset.ts`'s `layout !== 'none'` branch); the `ui` generator skips their `@blueprint-platform/ui/<name>` tsconfig path (no barrel — `FLAT_COMPONENTS` in `lib/resolve-deps.ts`) and warns if they land without their `core/theme|language` service. **(Superseded:** the old `layoutUiComponents` array + broad-copy-then-`tree.delete` approach in `preset.ts` is gone — see the starter-set bullet.) Import paths in the synced `.ts` are for the **generated-project** tree: switchers → `../../../core/theme|language/…`, `user-menu` → `../../../core/config/template-config`, the four shells' `LanguageService` import → `../../core/language/language.service`. `patch-app-config.ts` (**`showLanguageSwitcher`-gated** as of `0.1.20`; was layout-gated in `0.1.19`) adds `import { Directionality } from '@angular/cdk/bidi'` + `import { LanguageService }` and `{ provide: Directionality, useExisting: LanguageService }` to `app.config.ts` providers, so CDK overlays track a runtime language switch (`LanguageService implements Pick<Directionality,'value'|'change'>`). `template-config.ts.template` gained `showThemeSwitcher` / `showLanguageSwitcher` in both `BlueprintConfig` and `DEFAULT_BLUEPRINT_CONFIG` (EJS `<%= %>`), passed through the `files/config` `generateFiles` substitution; both also recorded in `.blueprint/manifest.json`. Verified end-to-end against Verdaccio (`0.1.11`, `latest` tag only — `stable` still `0.1.10`): `layout=sidebar-shell` (switchers both true) and `layout=topbar-shell` (`showThemeSwitcher=false`) both generate + `tsc -p tsconfig.app.json --noEmit` clean; `layout=none` generates with `core/theme/`, `core/language/`, and the three layout-UI folders all absent, no dangling aliases, `tsc` clean. Not yet done: `nx serve` runtime check that the switchers toggle the `dark` class / `dir` attribute (Task 7.4), and promoting the `stable` dist-tag (Task 7.6). The `create-nx-workspace` ≥ 23.1.1 `@nx/angular:application` failure noted below **did not reproduce** in this run — generation succeeded for all three cases (passing `--name=` explicitly); the slow part was Verdaccio proxying the full dep tree on a cold npm cache.
- ✅ Layout selection (`layout` option, originally foundation `0.1.11`; **generation-time handling now composes into the standalone `foundation:layout` generator, see the generator-kit bullet below** — `patch-app-routes.ts` is gone, superseded).
- ✅ Starter UI set + on-demand `ui` generator (foundation `0.1.15`, components `0.1.1`; both `latest` + `stable` on Verdaccio; e2e-verified). **All ~64 spartan-derived UI component sources moved out of foundation** to `packages/components/src/generators/ui/files/shared/ui/` — `foundation` no longer carries `files/shared/ui/` and its `files/shared/ui` assets glob is gone. The components pillar has one generator, `ui` (`nx g @blueprint-platform/components:ui <name…>` or `--all`): resolves the transitive `@blueprint-platform/ui/*` closure (regex scan) + flat-family siblings, `generateFiles`-copies each component **not already present** (idempotent), writes its `@blueprint-platform/ui/<name>` tsconfig path (skips the flat family `user-menu`/`theme-switcher`/`language-switcher`), adds **only** the npm deps the copied set imports (`lib/npm-deps.ts` version map — `@spartan-ng/brain`, `@ng-icons/*`, `class-variance-authority`, `clsx`, `tailwind-merge`, `embla-carousel*`, `ngx-scrollbar`), merges the names into `.blueprint/manifest.json`, and warns if a switcher lands without its `core/theme|language` service. `preset.ts` now: `require('@blueprint-platform/components/generators/ui').default` (components is a foundation **dependency** so it installs with the preset, and is written into the generated project's **devDependencies** at `compatibility.components` so more can be added later); calls it once for `STARTER_UI` (a const in `preset.ts` = `utils` + all form controls + the overlay/"popup" family; closure pulls `calendar` + `separator` — ~30 components) with `skipFormat`/`skipInstall`; and, when `layout !== 'none'`, again for the selected shell's own `@blueprint-platform/ui/*` imports (scanned from `files/layout/<layout>/*.ts`) ∪ `user-menu`/`theme-switcher`/`language-switcher`. `preset.ts` keeps only `@angular/cdk` + the Tailwind toolchain in the generated `package.json`; every other UI npm dep now comes from `ui`. `manifest.components` = the actual installed list. The old broad `generateFiles(files/shared/ui)` + `layoutUiComponents` `tree.delete` + `readdirSync` tsconfig-path loop are **removed** from `preset.ts`. New unit tests: `packages/components/src/generators/ui/ui.spec.ts` (8, all green). e2e (Verdaccio, `NX_WORKSPACE_ROOT_PATH` unset): `layout=sidebar-shell` → 37 components + `nx build` OK; `layout=none` → 30 (no layout extras, no `core/theme`); `nx g …:ui carousel` → adds `carousel` + `embla-carousel*`, idempotent, `nx build` OK; `nx g …:ui --all` → 64, `nx build` OK. **Known limitation:** `nx g …:ui theme-switcher`/`language-switcher` in a `layout: none` project leaves a dangling import to `core/theme|language/…` (the generator warns; those services live in `foundation`, not `components`).
- ✅ The `create-nx-workspace` ≥ 23.1.1 `@nx/angular:application` "TypeScript setup / project references" failure was **entirely** the harness's `NX_WORKSPACE_ROOT_PATH=d:\blueprint-platform` forcing preset resolution back to the monorepo. Fix for local e2e: run `env -u NX_WORKSPACE_ROOT_PATH npx create-nx-workspace@23.1.1 … --registry=http://localhost:4873` from a dir **outside** `d:\blueprint-platform` (e.g. `d:\bp-e2e`). With that unset, generation succeeds for every layout.
- ✅ Welcome screen (foundation `0.1.16`): `files/welcome/welcome.ts` (standalone `Welcome` component, EJS `<%= name %>`, Tailwind/spartan-var styled, dark-ready) is generated to `src/app/welcome.ts` **always**. `patch-app-component.ts` (ts-morph, `lib/`) then removes the stock `NxWelcome` (import + `imports:` entry + deletes `nx-welcome.ts`, tidies `app.spec.ts`) and: for `layout: 'none'` → `app.html` = `<app-welcome />`, `app.ts` imports `Welcome`; for a layout → `app.html` = `<router-outlet />`, `app.ts` imports `RouterOutlet`, and `wrapRoutesUnderLayout` (`generator-kit`, called via `foundation:layout`'s `indexRoute` option) adds `{ path: '', loadComponent: () => import('./welcome')… }` as the shell route's **first child** so `/` shows the welcome inside the shell's `<router-outlet>`. (Note: before this, generated `app.html` had only `<app-nx-welcome>` and *no* `<router-outlet>` at all — the shell route never actually mounted; this fixes that.) Assets glob `files/welcome/**/*.ts` added to `foundation/package.json`.
- ✅ Environments + Orval + proxy + API interceptor (foundation `0.1.17`; **unconditional** — API access is not a layout concern, so nothing here is gated on `layout`). `files/environments/` → `src/environments/`: `environment.ts` (EJS `<%= apiUrl %>` / `<%= appVersion %>` — the only substituted one), `environment.{dev,test}.ts` (`production:false`), `environment.{stg,prd}.ts` (`production:true`), all four non-default files ship `apiUrl: ''` for the developer to fill in; plus `api-url.token.ts` — an `API_URL` `InjectionToken` (`providedIn:'root'`, `factory: () => environment.apiUrl`, self-registering, so no `app.config.ts` provider entry, unlike `BLUEPRINT_CONFIG`). `files/core-api/api.interceptor.ts` → `src/app/core/api.interceptor.ts` — functional `HttpInterceptorFn` that prefixes `API_URL` onto **root-relative** (`/…`) requests only (absolute URLs pass through). `files/root/` → project root: `orval.config.ts` (imports the plain `environment` module — Orval runs in Node and cannot read the token; `ORVAL_API_URL` env override; `client:'angular'`, `mode:'tags-split'`, output `./src/app/core/api`) and `proxy.config.json` (`/api` → the origin). **`apiUrl` is origin-only (scheme+host+port, NO path)** — `toOrigin()` in `preset.ts` strips any path/trailing slash before substitution — because Orval-generated methods already carry their own `/api/...` prefix (baked from the backend's route templates); a path suffix here would double it (`/api/api/...`). `patch-app-config.ts` also adds `provideHttpClient(withInterceptors([apiInterceptor]))`. `patch-project-environments.ts` adds `dev`/`test`/`stg`/`prd` **build + serve** configurations to `project.json` (each a `fileReplacements` swap of `environment.ts`) and `serve.options.proxyConfig`; **Angular 22 `@angular/build:application` configurations have no `extends`** (verified — no configuration inheritance in the architect), so `optimization`/`sourceMap`/`extractLicenses` are spelled out per config (dev/test = unoptimised+sourcemap, stg/prd = optimised+hashed). `package.json` gains `orval@^7.20.0` devDep + `build:*`/`serve:*`/`generate:api` scripts. New schema fields: `apiUrl` (default `""`), `appVersion` (default `"0.1.0"`); both also recorded in `.blueprint/manifest.json`. Assets globs `files/environments/**/*.ts` + `files/core-api/**/*.ts` added; `files/root/*.template` ride the blanket `**/!(*.ts)` rule. Patchers verified offline against a real generated `app.config.ts` / `project.json`; full Verdaccio e2e + live `/api` double-prefix network check (Task 9/10) still pending.
- ✅ All four pillars are `package.json`-inferred Nx projects — every manually-created `project.json` deleted (per the rule below). `nx.json` `release` still has `projectsRelationship: "independent"`.
- ✅ `@blueprint-platform/generator-kit` (`0.1.0`) + `foundation:layout` (foundation `0.1.25`, components `0.1.3`; see the ngx-translate bullet above for the tier-1/2/3 `LanguageService` split this surfaced and required) — extracted the repeated generator-authoring patterns into a real, tested, published library, and built the standalone on-demand counterpart to `preset.ts`'s `layout` option. `packages/generator-kit/src/lib/`: `appendProvider` (add named import(s) + push one provider expression into `app.config.ts`'s `appConfig` array — `imports` is a *list* of `{names, from}`, not one pair, because a single provider routinely needs several imports, sometimes several names from one module in one `import {a,b} from '...'`, sometimes names from different modules), `wrapRoutesUnderLayout` (wrap `app.routes.ts`'s routes as `children` of a lazy-loaded shell parent, with an optional `indexRoute` child — e.g. the welcome screen), `registerUiPaths` (write `tsconfig.json` `compilerOptions.paths` aliases for a given name list + `sourceDir`/`aliasPrefix` — takes an already-resolved name list, doesn't `readdirSync` itself), `appendToManifest` (idempotent, sorted `.blueprint/manifest.json` category-array push; **no-ops if the manifest doesn't exist yet** — see the preset-time caveat below). 30 unit tests across the four.
  - `foundation:layout` (`packages/foundation/src/generators/layout/layout.ts`) reads from the **same** `files/layout/` catalog `preset` uses (`joinPathFragments(__dirname, '../preset/files/layout')` — note the real relative depth from `dist/generators/layout/`, one level up then into the sibling `preset/` dir, not the deeper path a `packages/foundation/src/...`-shaped guess would suggest), validates `--name` against it (throws listing valid names), copies the shell + self-contains the flyout for sidebar/topbar (same logic `preset.ts` used to inline), reads the real exported class name back from the copied file, adds the shell's own UI imports + `user-menu`/`theme-switcher` (+ `language-switcher` **only if `core/language/` already exists** — this generator has no `showLanguageSwitcher` option of its own; it reflects whatever preset-time generation, or a later manual step, already decided) via the same `ui` generator, calls `patchUserMenu` to reconcile if not, wires the welcome screen as the shell's index route if `welcome.ts` exists, and records itself in the manifest. `preset.ts`'s own `layout !== 'none'` branch is now just `await layoutGenerator(tree, { name: layout })` — one implementation, two entry points (matches `components:ui`'s catalog-validation design).
  - `components:ui`'s own `shared/ui` tsconfig-paths block and manifest-merge block now call `registerUiPaths`/`appendToManifest` too (not just foundation) — that's where these patterns *actually* lived by the time this work started (an earlier task had already moved UI-copying out of `preset.ts` into `components:ui`), so generalizing them and leaving `components:ui` on the old inline duplicate would have missed the point. Verified via `components`'s existing 8-test suite staying green, unchanged.
  - **Critical fix this surfaced:** `preset.ts` used to **delete** `@blueprint-platform/foundation` from a generated project's `package.json` after generation. That made `nx g @blueprint-platform/foundation:layout` — the entire scenario this work exists for — impossible to run later, since `foundation` was never even installed. It's now kept as a **devDependency** (version read from foundation's own `package.json` at generator runtime, `../../../package.json` relative to `dist/generators/preset/`, same depth as the existing `compatibility.json` read — not from `compatibility.json` itself, which has its own stale, never-updated `foundation` field), mirroring how `components` is already kept. e2e-verified end to end: generate `--layout=none`, confirm empty `layout/`, run `nx g @blueprint-platform/foundation:layout --name=sidebar-shell` against it, confirm the shell + its UI deps + `app.routes.ts` (welcome index route included) + `.blueprint/manifest.json` `layouts: ['sidebar-shell']`, then `nx build` green.
  - **Known, accepted deviation from strict byte-identical output:** refactoring `patch-app-config.ts` to `appendProvider` calls in natural push order changes the *import declaration* line order slightly relative to the pre-refactor hand-written code (whose import-call order and push-call order were never fully consistent with each other to begin with) — same imports, same provider array order, zero functional difference, confirmed by direct diff of captured before/after output.
  - Also fixed in passing: `packages/foundation/jest.config.cts` was picking up `files/environments/environment.test.ts` (a real *template* file, one of the `environment.<env>.ts` set) as a Jest test file, because it matches jest's default `**/*.test.ts` testMatch and nothing excluded `files/` payload from test discovery — added `testPathIgnorePatterns` for `src/generators/**/files/`.
- 🟡 `@blueprint-platform/generator-kit` `0.1.2` + `@blueprint-platform/modules` `0.1.0`'s `auth` generator (`nx g @blueprint-platform/modules:auth`) — the first `modules` pillar generator, and the first cross-pillar composition beyond `foundation`↔`components`. Three options: `authType` (`'jwt' | 'session'`, default `'jwt'`), `storeType` (`'local' | 'memory'`, default `'local'`), `authLayout` (`'split' | 'centered'`, default `'split'`). **Marked 🟡 not ✅** — all code is written, built, and unit-tested, and published to local Verdaccio (`generator-kit@0.1.2`, `foundation@0.1.26`, `modules@0.1.0`, all tag `latest`, none tagged `stable` yet), but a full live e2e pass (Task 11 of `tasks/task-auth-module-platform-sync-v2.md`) has **not** been completed end-to-end — see the last bullet below.
  - **`protectedRouteBranches` (`generator-kit` `0.1.1`) — the reason this exists.** `foundation:layout`'s `wrapRoutesUnderLayout` (used when adding the ONE main app shell) sweeps every existing top-level route into that shell's children — correct for a main shell, wrong for a branch like `auth` that must stay a sibling forever regardless of when a main shell is added. `generator-kit`'s new `addLayoutBranch(tree, appRoot, { path, layoutImportPath, layoutClassName, children })` adds a brand-new top-level route (never wraps/touches existing ones — genuinely different from `wrapRoutesUnderLayout`) and records `path` in `.blueprint/manifest.json`'s `protectedRouteBranches` array (idempotent, default `[]`, alongside `components`/`modules`/`layouts` — `preset.ts` now writes this field too). `wrapRoutesUnderLayout` reads that array and excludes any matching top-level route from the sweep, re-adding it as its own sibling after the new shell — verified end-to-end: generate `--layout=none`, run `modules:auth` (registers `auth`), then run `foundation:layout --name=sidebar-shell` — the `auth` branch is confirmed to land OUTSIDE `sidebar-shell`'s `children`, untouched. `children` entries are recursive (`{ path, componentImportPath?, componentClassName?, redirectTo?, pathMatch?, children? }`) — needed for `auth`'s own nested `forgot-password/{email,code,new}` sub-flow.
  - **`modules:auth` composes into `foundation:layout` rather than owning layout content.** The two auth layouts (`auth-split`, `auth-centered` — two-pane brand panel / single centered card, each just a `<router-outlet/>` + spartan `hlmCard`) were moved into `foundation`'s own `files/layout/` catalog (`packages/foundation/src/generators/preset/files/layout/auth-split/`, `.../auth-centered/`, folder+class named `AuthSplit`/`AuthCentered`, no `-shell` suffix — these are NOT one of the four main app shells and don't count against that naming convention) so `modules:auth` can call the exact same `foundation:layout` generator function every main shell uses, via a new `"./generators/layout"` export on `foundation`'s `package.json` (mirrors `components`'s existing `"./generators/ui"` export — a deep `require()` into a published package needs an explicit `exports` entry or Node blocks it) and a local `loadLayoutGenerator()` shim in `packages/modules/src/generators/shared/` (mirrors `foundation`'s own `load-ui-generator.ts`).
  - **`foundation:layout` gained two options *because of* this reuse, both defaulting to the old (main-shell) behavior:** `wireRoutes` (default `true`; `false` skips the `wrapRoutesUnderLayout` sweep + welcome-index-route wiring — a content-only shell like an auth layout must never become the app's root wrapper) and `withAccountMenu` (default `true`; `false` skips adding `user-menu`/`theme-switcher`/`language-switcher` — an auth page has no signed-in user yet). `modules:auth` calls `layoutGenerator(tree, { name: 'auth-split'|'auth-centered', wireRoutes: false, withAccountMenu: false })`, then wires its own `auth` route branch via `addLayoutBranch`. `preset.ts`'s own call (`layoutGenerator(tree, { name: layout })`) is unaffected — both new options default to the behavior it already relied on.
  - **Auth module file layout**, `packages/modules/src/generators/auth/files/`: `core/auth/` (→ generated project's `src/app/core/auth/`) — `models.ts`, `auth-mock.data.ts` (the fake backend — no real `HttpClient` calls), `token-store.ts` + `local-storage-token-store.ts` + `in-memory-token-store.ts` (storage seam, swapped by `storeType`), `provide-auth.ts`, and **both** `jwt-auth.{service,interceptor}.ts` and `session-auth.{service,interceptor}.ts` are copied, then the one `authType` didn't select is `tree.delete`d (copy-then-delete, same idiom `foundation:layout` uses for the dangling `language-switcher`) — simpler than filtering `generateFiles`. `features/auth/forms/` (→ `src/app/features/auth/forms/`) — `password-match.validator.ts` + six form components (login, signup, three-step forgot-password, change-password), copied **unconditionally regardless of parameters**. Only `login-form.ts.template` varies with `authType` (EJS-conditional import + `inject()` of `JwtAuthService` vs `SessionAuthService`) — every other form only calls the mock backend or a validator directly, so needed no templating. (Deviation from `blueprint-reference`: its `login-form.ts` hard-injects **both** services with a `?strategy=` query-param toggle for previewing both at once side-by-side; a generated project only ever has one strategy installed, so that toggle doesn't carry over.)
  - **UI dependencies are computed, not hardcoded**, same discipline as everywhere else: a new `scanUiImports` (`packages/modules/src/generators/auth/lib/scan-ui-imports.ts`, `generator-kit`'s `ui`-copy scan generalized to walk recursively since forms live one directory deeper than a flat layout-shell folder) finds every `@blueprint-platform/ui/<name>` the copied forms actually import (`alert`, `spinner`, `card`, `button`, `field`, `input`, `input-group`, `checkbox`, …— several of these, e.g. `alert`/`spinner`/`card`, are NOT in `preset`'s `STARTER_UI` set) and calls `components:ui` with that list; `card` for the auth-split/centered shell itself is separately picked up by `foundation:layout`'s own existing `shellUiImports` scan. `packages/modules/package.json` needed a direct `@blueprint-platform/components` dependency for this (beyond the `foundation`/`generator-kit` the original task spec called for) — it's genuinely required at generator runtime, not just transitively available.
  - **`app.config.ts` gets two separate `appendProvider` calls for its `providers:` array entries**, not a merge into `foundation`'s existing `provideHttpClient(withInterceptors([apiInterceptor]))` *call*: one for `provideAuth()` / `provideAuth({ tokenStore: 'memory' })` (per `storeType`), one for a **second** `provideHttpClient(withInterceptors([jwtAuthInterceptor]))` (or `sessionAuthInterceptor`). `HTTP_INTERCEPTOR_FNS` is a multi-provider token so two `provideHttpClient(...)` calls are functionally additive — deliberately chosen over teaching `generator-kit` how to merge into an existing array-literal *call expression*, which no task actually needed elsewhere.
  - **Real bug this surfaced, fixed in `generator-kit` `0.1.2`: `appendProvider` used to always emit a brand-new `import { ... } from '<module>'` declaration, even when `app.config.ts` already had one for that exact module.** Since both `foundation`'s own `patch-app-config.ts` (for `apiInterceptor`) and `modules:auth` (for `jwtAuthInterceptor`/`sessionAuthInterceptor`) need `provideHttpClient`/`withInterceptors` from `@angular/common/http`, the second `appendProvider` call produced a **second** `import { provideHttpClient, withInterceptors } from '@angular/common/http'` line — a TS2300 "Duplicate identifier" compile error, only caught by an actual `nx build`, not by any of the tree-based unit tests (they never invoke the Angular/TypeScript compiler). Fixed by having `appendProvider` look for an existing import declaration from the same module first and merge in only the not-yet-present names (`existing.addNamedImports(...)`) instead of unconditionally calling `file.addImportDeclaration(...)`. This is a general fix, not an auth-specific patch — any two independent `appendProvider` callers needing the same module now compose safely. New tests in `append-provider.spec.ts` cover both the merge and the already-present no-op case. **Lesson: a tree-based generator unit test proves the file was *edited*, not that the result *compiles* — only an actual `nx build` (or full e2e) catches cross-call import collisions like this one.**
  - **Global styles**: `patch-global-styles.ts` appends two general-purpose (not auth-specific) rules found while diffing `blueprint-reference`'s `styles.scss` during this work — an `hlm-input-group` inner-ring override and a `-webkit-autofill` neutralizer — onto the generated project's `src/styles/_utilities.scss`, idempotently. Not yet folded into `foundation`'s own copy of that file directly, since they were discovered here, not there.
  - **Not run twice.** `modules:auth` checks `.blueprint/manifest.json`'s `modules` array first and no-ops with a log message if `'auth'` is already there — neither `addLayoutBranch` nor `appendProvider` de-duplicate on their own (by design — see the generator-kit bullet), so the caller has to.
  - 15 unit tests (`packages/modules/src/generators/auth/auth.spec.ts`) + `generator-kit`'s expanded suite (43 total: `add-layout-branch.spec.ts` new; `wrap-routes-under-layout.spec.ts` +2 for the protected-branch exclusion; `append-provider.spec.ts` +2 for the import-merge fix above) all green, exercising the real cross-package `require()` chain (built `dist/` of `components`/`foundation`/`generator-kit`, not mocks).
  - **e2e status (Task 11): one clean live pass completed the structural/content checks, but a full green-build pass across all three scenarios has NOT been confirmed yet.** Sequence so far: published `generator-kit@0.1.1`/`foundation@0.1.26`/`modules@0.1.0` to local Verdaccio → ran `create-nx-workspace` + `nx g modules:auth` for both parameter combinations (`jwt+local+split`, `session+memory+centered`) against `http://localhost:4873` → every structural assertion passed (file presence/absence per `authType`, route branch + nested `forgot-password`, manifest fields, provider wiring, UI deps) → `nx build` failed on **both** with the `appendProvider` duplicate-import bug above → fixed and republished `generator-kit@0.1.2` → a second full e2e re-run was started but had to be aborted mid-flight (unrelated tooling issue: overlapping background runs on Windows, see below) before a green `nx build` was confirmed. **Still needed before promoting any `stable` tag:** rerun all of Task 11.2 (both combos, expect green `nx build` now) and Task 11.3 (the ordering scenario — `layout:none` → `modules:auth` → `foundation:layout --name=sidebar-shell`, confirming `auth` survives outside the new shell's children) in one clean pass.
  - **Operational note for whoever runs that e2e next:** `create-nx-workspace`/`nx new` spawns detached grandchild `node.exe` processes on Windows that outlive both the parent shell and a harness-level "stop" on the wrapping task — killing the top-level process is not sufficient. Before treating an e2e run as finished, cancelled, or safe to re-run, check for and kill by PID (e.g. via `Get-CimInstance Win32_Process | Where CommandLine -like '*create-nx-workspace*'`), not just by stopping the launching task — otherwise concurrent runs race on the same output directory and silently corrupt each other's results (this happened once during this work).
- ✅ Flag-gated auth forms (`modules` `0.1.1`; `tasks/task-auth-optional-forms.md`) — three new independent `modules:auth` schema options on top of the above, sized so a future CLI multi-select maps one checkbox per flag: `includeSignup` (default `true`), `includeForgotPassword` (default `true`, gates all three forgot-password steps — email/code/new-password — **together as one unit**, since a linear flow with only some steps present doesn't make sense standalone), `includeChangePassword` (default `true`, a genuinely separate flow from forgot-password). **`login-form` has no flag of its own — it's the one form that's always present**, unconditionally, in every generated auth setup; all three defaults are `true` specifically so nothing changes for an existing caller until a flag is explicitly set `false`.
  - **The `AUTH_FEATURES` token — why it exists.** The always-present `login-form` links to `signup` and `forgot-password`, but those routes might not exist if their flag is `false` — a dangling link to a route that was never generated is a real, user-facing bug, not cosmetic. `core/auth/auth-features.token.ts.template` (the **only** file in the whole auth module needing `.template` EJS substitution beyond `login-form.ts.template`'s existing `authType` one) is a small `InjectionToken<AuthFeatures>` (`{ signup, forgotPassword, changePassword }` booleans), `providedIn: 'root'` + factory baked from the three flags at generation time — same self-registering pattern as `API_URL`/`BLUEPRINT_CONFIG`, no `app.config.ts` provider entry needed. `login-form.ts`/`.html` inject it and wrap both links in `@if (authFeatures.signup)` / `@if (authFeatures.forgotPassword)`; **`login-form` itself otherwise stays a zero-edit copy** — the conditional lives once, in its own template, never templated per-flag. **Found while implementing:** `blueprint-reference`'s `login-form` had no "Sign up" link at all yet (the task's own wording assumed one already existed) — added one (`Don't have an account? Sign up`) as part of this work, in both `blueprint-reference` and the generator's copy.
  - **Conditional copying + route wiring, same "copy then delete" idiom as the strategy pair.** `auth.ts` copies the whole `files/features/auth/forms` tree unconditionally (simpler than teaching `generateFiles` a per-subfolder skip list — same reasoning as the jwt/session copy-then-delete), then `tree.delete`s `signup-form` / the three `forgot-password-*-form` folders together / `change-password-form` per their flag. The `addLayoutBranch` `children` array is built the same way: `login` always first, then each optional block `.push()`ed only if its flag is true — real exported class names are still read from the actual copied files via `readClassName`, never assumed, even for the conditionally-included ones.
  - **Manifest:** the generic `modules` array (the idempotency check + every pillar's shared push-a-name convention, via `appendToManifest`) stays a plain `['auth']` — it isn't the place to grow a module-specific object. A new `manifest.authConfig` field (`{ name: 'auth', authType, storeType, authLayout, features: { signup, forgotPassword, changePassword } }`), written directly via `updateJson`, carries the richer per-module record instead.
  - 8 new unit tests in `auth.spec.ts` (23 total) covering: always-present login-form/token regardless of flags, each flag's own copy/route-omission, the substituted token content, and the full `authConfig` shape — all green, plus the pre-existing 15 unrelated to this change still passing (no regression). `nx build modules` clean; the `.template` token file confirmed present in `dist/generators/auth/files/core/auth/` (rides the blanket `**/!(*.ts)` asset rule, same as every other `.template` in this project — no new asset glob needed). Published `modules@0.1.1` to local Verdaccio (`latest` tag; `compatibility.json`'s `modules` field bumped to match).
  - **Real bug this surfaced, fixed: `preset.ts` pins `@blueprint-platform/modules`'s generated-project devDependency version from foundation's OWN `compatibility.json`, snapshotted at foundation's last publish — bumping the local `compatibility.json` file does nothing until foundation itself is rebuilt and republished.** First e2e pass generated every project with `"@blueprint-platform/modules": "0.1.0"` hardcoded (from `foundation@0.1.26`'s frozen `compatibility.json`), so `nx g modules:auth` always ran the OLD generator regardless of what `latest` pointed to on Verdaccio or npm cache state — a red herring that looked like a stale-cache problem at first (clearing `npm cache` changed nothing, confirming it). Fixed by bumping foundation to `0.1.27` (code unchanged, only `compatibility.json`'s `modules: "0.1.0" → "0.1.1"`), rebuilding, republishing. **Lesson: any patch that only touches a pillar's `compatibility.json` (no source change) still requires a version bump + rebuild + republish of that pillar itself** — the file has no effect on already-published tarballs.
  - **e2e (Task 0's browser check aside): two cases, both fully green** — `a-defaults` (all three flags default `true`) and `b-login-only` (`--includeSignup=false --includeForgotPassword=false --includeChangePassword=false`), each via real `create-nx-workspace@23.1.1` + `nx g @blueprint-platform/modules:auth` against Verdaccio (`http://localhost:4873`) + real `nx build`. Every structural assertion passed for both: form presence/absence per flag, `auth-features.token.ts` always present with the correct substituted booleans, `login-form.ts`/`.html` always injecting/reading `AUTH_FEATURES`, route presence/absence in `app.routes.ts` (login route always present even with everything else off), `manifest.authConfig.features` matching the requested flags exactly, `manifest.modules` staying the plain `['auth']` unaffected. `nx build` green on both.
  - **`blueprint-reference` preview note:** the auth-preview's new signup/forgot/change-password toggle chips can't just mutate the injected `AUTH_FEATURES` value in place (it's plain booleans, generation-time-fixed by design, not reactive) — toggling instead destroys and recreates a thin `<router-outlet>` wrapper component (`AuthOutletScope`) whose component-level `AUTH_FEATURES` override re-reads a preview-only signal store fresh each time it's recreated. **Live browser pass (spacing/alignment with a link missing) still owed — no browser in this session's environment**, logged as such in `blueprint-reference/SYNC_LOG.md`'s prep entry, matching the same caveat on the earlier dark-theme/switchers prep entry.
- ✅ Logo + favicon branding (foundation `0.1.27` → next patch; `tasks/task-logo-integration.md`. **Wiring + visual placement only — deliberately not build/published/e2e'd; that's a separate follow-up task list before promoting `stable`.**)
  - **Real design assets, not templates.** `packages/foundation/src/generators/preset/assets/favicon.ico` (multi-res, 7 images) and `assets/branding/logo.png` (1440×1050 RGBA "bp" monogram, transparent background) — a genuinely different top-level source folder from every `files/**` payload tree in this generator (`assets/`, not `files/`), specifically so it's never swept up by a `files/**`-scoped glob rule and to make the binary-vs-template distinction obvious at a glance.
  - **Why `assets/` isn't `files/`, and why the copy is `Buffer`-based, not `generateFiles`.** `generateFiles` parses every file it copies as an EJS template — safe for `.ts`/`.scss`/`.json`/`.md`, unsafe for arbitrary binary image bytes (a `<%`-lookalike byte sequence in binary data risks a bad parse, mis-re-encode, or corruption). `preset.ts` instead does a direct `readFileSync`/`tree.write` `Buffer` copy for exactly these two files, unconditionally (branding is foundation-level, not gated behind `layout` or any other option) — placed right after `applicationGenerator` scaffolds the app, since Angular's own default scaffold already creates a placeholder `public/favicon.ico` (and `index.html`'s `<link rel="icon" type="image/x-icon" href="favicon.ico">`) that this overwrites rather than duplicates. `Tree.write`'s `content` param is typed `Buffer | string` — confirmed by reading `node_modules/nx/dist/src/generators/tree.d.ts` directly rather than assuming, then verified with a real build: `sha256sum` of the source `assets/` files vs. the copied `dist/generators/preset/assets/` files matched exactly, and the PNG/ICO magic bytes (`89 50 4E 47…` / `00 00 01 00…`) were intact in `dist`.
  - **Explicit asset glob, not reliance on the existing blanket rule.** `foundation/package.json`'s `nx.targets.build.options.assets` gained `{ input: ".../generators/preset/assets", glob: "**/*.{ico,png}", output: "./generators/preset/assets" }` — even though the existing blanket `**/!(*.ts)` rule (from `./packages/foundation/src`) would likely already have matched these files too (neither ends in `.ts`), this project's specific history of glob rules silently not matching (see the stray-`project.json` and `files/**` tsconfig-exclude gotchas elsewhere in this doc) made an explicit rule the safer choice per the task's own instruction, and it was verified rather than assumed.
  - **Existing placeholder branding found and replaced, not duplicated (Task 0 discovery).** All four main shells (`sidebar-shell`, `floating-shell`, `inset-shell`, `topbar-shell`) already had an identical header brand slot — a `size-8` `bg-sidebar-primary` square containing a literal `"B"` character, beside a `"Blueprint"` title/subtitle — inside `hlmSidebarHeader`. The `<img>` **replaces** that square div (not adds alongside it), keeping the same flex slot; sizing is `h-8 w-auto` (not the task sketch's literal `size-8` fixed-square + rounding), discovered only after actually looking at the shipped `logo.png` — it's a wide monogram mark (~1.37:1), not a square icon, so forcing it into a square with `object-contain` would just letterbox it smaller for no reason. `auth-split`/`auth-centered` had no image-based brand mark, only a text `{{ brand }}` binding (kept as-is — it's marketing copy woven into the panel/footer text, not a competing logo mark); the new `<img>` was added **above the form card in `<main>`**, per the task's own guidance, specifically because `auth-split`'s brand `<aside>` is `hidden` below `md` — a logo placed only there would be invisible on narrow screens, while placing it above the card keeps it visible at every width, in both auth layouts uniformly.
  - **Sync discipline followed as usual:** placed and visually reasoned about in `blueprint-reference` first (`ng build` clean both before and after the `h-8 w-auto` sizing fix; a copy of the same `logo.png`/`favicon.ico` was placed under `blueprint-reference/public/` purely so the reference app can actually render them — that copy is not itself synced anywhere, it's the reference app's own asset), then the identical diff applied to all six `packages/foundation/src/generators/preset/files/layout/*/` copies (`diff` confirmed byte-identical after sync, same as every prior layout sync in this project) — no path translation needed for the `src="branding/logo.png"` attribute, since both repos use the same `public/`-relative convention. No `SYNC_LOG.md` entry for this one — the task explicitly scoped it as a lighter "does it look right" check, not a full sync-ready pass (no RTL/palette matrix, no live browser confirmation — same "no browser in this environment" limitation noted elsewhere).
  - **Task 6 (favicon `<link>` in `index.html`) needed no code change** — confirmed against a real generated project (from the auth-flags e2e run above): `@nx/angular`'s `applicationGenerator` already scaffolds `<link rel="icon" type="image/x-icon" href="favicon.ico">` pointing at exactly `public/favicon.ico`, which this task's `Buffer` copy now overwrites with the real icon.
  - **Explicitly not done here, by design:** no version bump/rebuild/republish of `foundation`, no Verdaccio publish, no scratch-project generation to click through the logo in a live served app. `nx build foundation` was run repeatedly to verify the asset pipeline itself (glob matching, `Buffer` fidelity), and `nx test foundation` still shows only the one pre-existing, already-documented `preset.spec.ts` failure (`applicationGenerator` under jest) — no new regressions. A follow-up task list should cover the full build/publish/generate/serve/screenshot cycle before promoting any `stable` tag.
- ✅ RBAC → `@blueprint-platform/modules` (`modules` `0.1.3`, `generator-kit` `0.1.3`, `foundation` `0.1.31`; `tasks/task-rbac-platform-sync.md`) — the second `modules` pillar generator, `modules:rbac`. **Fully decoupled from `modules:auth` in both directions** — no dependency on `@blueprint-platform/foundation` even (unlike `auth`, `rbac` never composes into `foundation:layout`, since it has no layout of its own), no import of anything under `core/auth` anywhere in its copied output, verified by e2e generating it completely standalone (no auth module present at all) and by both orders alongside `auth` (`auth`→`rbac` and `rbac`→`auth`), all three with a real `nx build` green.
  - **The core design change: `CURRENT_USER_PERMISSIONS`, an injection token, is RBAC's *only* integration point with any auth setup.** The old reference-only `CurrentUserPermissionsService` read the signed-in user's id straight out of `core/auth`'s `TokenStore` — exactly the coupling that couldn't exist in the platform version. Decoupled in `blueprint-reference` first: `core/rbac/current-user-permissions.token.ts` defines `CurrentUserPermissionsResolver` (`hasPermission(key): boolean`) and the `CURRENT_USER_PERMISSIONS` token (`providedIn: 'root'`, factory defaults to `RbacDemoSessionService`); `permission.guard.ts` now injects the token, never a concrete service. A real project overrides the token in its own `app.config.ts` (`{ provide: CURRENT_USER_PERMISSIONS, useClass: MyRealPermissionsService }`) — documented right in the token file. `modules:rbac` needs **zero `app.config.ts` provider patch** for the default to work, since it's self-registering.
  - **`RbacDemoSessionService`** — RBAC's own lightweight "active role" concept (a `signal<string|null>`, seeded with the *first* role in the roles list, not `null`, so a fresh project isn't locked out of every gated route by default), genuinely useful on its own, not a placeholder: it's what lets the RBAC preview demo permission-gated content by switching roles with **no sign-in step at all**. This is a real behavior change from the pre-sync reference preview, which required signing in via `authGuard` first — the preview's `authGuard`/`JwtAuthService`/`SessionAuthService`/"sign out" button were all removed and replaced with a role-switcher toolbar control, specifically so `features/rbac/` (preview included) has zero auth reference anywhere, per the task's own Task 1.5 grep check.
  - **Real coupling found beyond what the task called out: every RBAC list/form component hard-coded the reference-only absolute path `/rbac-preview/...` for navigation** (the "new"/"edit" links, each form's "Cancel" link, and the post-submit redirect) — a naive 1:1 copy would have left every one of these broken under the generator's configurable `routePrefix`. Fixed in `blueprint-reference` first by switching to relative navigation (`routerLink="new"`, `[routerLink]="[id, 'edit']"`, and — since a permission's edit route is nested two levels deeper than its "new" route (`:id/edit` vs `new`) — `router.navigate(isEditMode ? ['..','..'] : ['..'], { relativeTo: this.route })` in both the template Cancel link and the post-submit handler). This is also *why* `appendChildRoutes`'s route tree preserves the reference's exact nested shape (`permissions` → `''`/`new`/`{':id'→'edit'}`) rather than flattening to single path strings the way the task's own sketch first suggested — relative navigation depends on route-CONFIG nesting depth, not just the URL string, so the generated tree has to match the reference 1:1 for the fix to actually work in both places.
  - **New `generator-kit` helper: `appendChildRoutes`** — a third, genuinely distinct routing helper (`wrapRoutesUnderLayout` wraps *everything* under a *new* shell; `addLayoutBranch` adds a *new top-level sibling* that must stay outside any shell forever) for routes that belong *inside* whatever main shell already exists, as ordinary feature content. Finds the "main" route the same way `wrapRoutesUnderLayout` finds what to exclude — the one top-level entry with its own `children` whose `path` is NOT in `protectedRouteBranches` — and pushes into its `children`, or as flat top-level entries if there's no shell at all (`layout: 'none'`). Reuses `LayoutBranchChild` (now recursive, not the task sketch's flat-only shape — see above) and the `renderChild` serializer, newly exported from `add-layout-branch.ts` rather than duplicated.
  - **Destination mapping, 1:1**: `core/rbac/*` (models, services, guard, token, demo session — **excluding** `permission-graph.spec.ts`; no precedent anywhere in this project for shipping test files as generator payload) → `src/app/core/rbac/`; `features/rbac/{forbidden,permissions,roles}/*` → the same paths under `src/app/features/rbac/` — **not** `features/rbac/preview/` (never synced) and **not** `rbac.routes.ts` (the route tree is built programmatically in `rbac.ts` via `appendChildRoutes`, never shipped as a file).
  - **`routePrefix` (default `'admin'`)** — the one configurable option, deliberately minimal (RBAC has no auth-style runtime-strategy branch to parameterize). Mounts `<routePrefix>/permissions`, `<routePrefix>/roles`; `forbidden` is always mounted OUTSIDE that group, at the fixed top-level `/forbidden`, since `permissionGuard`'s redirect target (`router.parseUrl('/forbidden')`) is absolute and independent of whatever prefix was chosen.
  - **Real bug found during e2e, fixed: `rbac.ts` initially never added the admin pages' own UI-component dependencies** (`accordion`, `alert`, `alert-dialog`, `badge`, `table`, …) — `nx build` failed on all three e2e scenarios with `Cannot find module '@blueprint-platform/ui/...'`. Fixed by promoting `scanUiImports` out of `auth/lib/` into `modules/src/generators/shared/` (both generators use the identical "scan copied `.ts` for `@blueprint-platform/ui/*` imports, call `components:ui` with that list" pattern — sharing it rather than duplicating is the same discipline `generator-kit` itself exists for) and calling it in `rbac.ts` the same way `auth.ts` does. **Lesson, again: a tree-based generator unit test proves files were copied, not that the result compiles** — this needed the actual e2e `nx build` to surface, same category of miss as the `appendProvider` duplicate-import bug during the auth sync.
  - **Separately found and fixed while investigating a live runtime error the user hit clicking a roles-list row action ("view assigned users" → `hlm-alert-dialog.open()` → `TypeError: this._directionality.valueSignal is not a function`): both `LanguageService` variants (`files/language/language.service.ts` and `.translate.ts`) were missing `readonly valueSignal = this.dir`.** `Directionality` (which `LanguageService` stands in for via `{ provide: Directionality, useExisting: LanguageService }`) has a *public* `valueSignal: WritableSignal<Direction>` member that spartan's brain primitives (`accordion`, `dialog`, `hover-card`, `navigation-menu`, `overlay`, `radio-group`, `resizable`, `slider`, `sonner`, `tabs`, `tooltip`) read directly, not just the `value` getter — omitting it isn't a TS error (it's outside the `Pick<Directionality, 'value'|'change'>` the class declares), only a runtime one, so it slipped past every prior build-level check. `blueprint-reference`'s own `LanguageService` (now at `shared/language.service.ts`) already carried this exact fix; it never got ported to the platform's two-variant (base/translate) split when that was built. Fixed by adding the same `valueSignal = this.dir` alias (read-only, aliased straight to the existing `dir` computed — none of spartan's internals ever call `.set()`/`.update()` on it) to both variants, republished as `foundation@0.1.31`. **Any project generated before this fix has the bug baked into its own `src/app/core/language/language.service.ts`** — regenerating fresh (or manually adding the one line) is required to pick it up; there's no way for an already-generated project to inherit a generator-side fix automatically.
  - 9 new unit tests in `rbac.spec.ts` + 5 new in `generator-kit`'s `append-child-routes.spec.ts` (81 total across the two packages), all exercising the real cross-package `require()` chain. e2e: three scenarios (standalone/no-auth, `auth`→`rbac`, `rbac`→`auth`), every structural assertion plus a real `nx build` green on all three.
- ✅ User management → `@blueprint-platform/modules` (`modules` `0.1.4`, `foundation` `0.1.32`; `tasks/task-user-management-platform-sync.md`) — the third `modules` pillar generator, `modules:user-management`. **The simplest of the three so far, and zero dependency on either `modules:auth` or `modules:rbac`, in either direction** — confirmed by grep (only doc-comment mentions of `core/auth`/`core/rbac` survive, explaining the deliberate absence, never a real import), no `@blueprint-platform/foundation` dependency (no layout of its own, doesn't even need `foundation:layout` the way `rbac` needed it for nothing), and no `app.config.ts` provider patch at all (no token, no interceptor, no guard this module needs).
  - **`ManagedUser` / `UserService` / `UserStorage`** — same mock-persistence shape as `auth`/`rbac` (`UserStorage`, a fresh independent `localStorage` wrapper, NOT a reuse of `RbacStorage` — the reference's own doc comment is explicit that an accidental dependency edge here is worse than a few duplicated lines). `accountRole` is deliberately a plain string label (`'Admin'|'Manager'|'Staff'|'Viewer'`, from a static `ROLE_OPTIONS` list), not named `role` and with zero relation to RBAC's `Role` entity — the naming difference is intentional, to avoid even a conceptual echo of that other module.
  - **`details-user` is a dialog, not a route** (confirmed via the reference file's own doc comment: "matching the list's own action pattern (deactivate/change-role are also dialogs, not routes)") — same discovery-over-assumption resolution `rbac`'s route-vs-dialog ambiguity needed. Only `UsersList`, `AddUser`, `EditUser` are routed; `DetailsUser`/`ChangeRoleDialog`/`DeactivateConfirm` are plain sibling components `users-list.ts` opens imperatively via `viewChild(...).open(user)`.
  - **The exact same hardcoded-absolute-path coupling RBAC hit, found again and fixed the same way.** Every navigation in `users-list.html` (the "new" link, the per-row "edit" link), `user-form.html` (the shared add/edit form's "Cancel" link), and `add-user.ts`/`edit-user.ts` (the post-submit redirect) hardcoded the reference-only `/user-management-preview/users` path. Fixed in `blueprint-reference` first with relative navigation — `routerLink="new"`, `[routerLink]="[id,'edit']"` in the list template; `[routerLink]="mode()==='edit' ? ['..','..'] : ['..']"` in `user-form.html`'s Cancel link (the one form component shared by both `add`/`edit` hosts, so the up-count has to depend on `mode()`, an existing `input.required<'add'|'edit'>()`); `router.navigate(['..'], { relativeTo })` / `router.navigate(['..','..'], { relativeTo })` in `add-user.ts`/`edit-user.ts`'s post-submit handlers. Same reasoning as RBAC: the generated route tree preserves the reference's exact nesting (`routePrefix` → `''`/`new`/`{':id'→'edit'}`) specifically so these relative-navigation depths keep working after sync.
  - **`routePrefix` (default `'admin/users'`)** — the one option, and unlike RBAC's `'admin'` (a parent prefix with two sibling feature areas — `permissions`/`roles` — nested under it), this module has exactly one feature area, so `routePrefix` **is** the list's own mount path directly, not a prefix something else nests under. No bare-prefix redirect needed (unlike RBAC's `''→'permissions'`) — visiting `routePrefix` itself already is the list (the tree's own `path: ''` empty-path child).
  - **Task 0.3 (was the avatar/thumbnail primitive already synced anywhere else?) — yes**, `avatar` already existed in `packages/components/src/generators/ui/files/shared/ui/avatar` from earlier work; this module is not the first place it lands, and needed no new sync work for it — `scanUiImports` (shared with `auth`/`rbac`, `modules/src/generators/shared/`) picks it up automatically alongside `badge`/`button`/`dropdown-menu`/`table`/`card`/`field`/`input-group`/`native-select`/`switch`/`alert-dialog`.
  - **Reuses `appendChildRoutes` (built for RBAC) completely unchanged** — no new `generator-kit` code needed for this sync, confirming it was generalized correctly the first time.
  - 9 new unit tests in `user-management.spec.ts` (42 total across `modules`), all exercising the real cross-package `require()` chain. `nx build` clean, dry-run `npm pack` confirmed the expected `dist/generators/user-management/**` contents before the real publish. **Per this task's explicit instruction: no e2e verification cycle this time** — no scratch-project generation, no click-through, no combination testing with `auth`/`rbac`. Published `modules@0.1.4` to local Verdaccio and tagged both `latest` **and** `stable` on the strength of a clean build alone (not a green e2e `nx build` in a real generated project, unlike `auth`'s and `rbac`'s `stable` promotions, which are still gated on that and not yet promoted) — `foundation` bumped to `0.1.32` for the `compatibility.json` `modules` field (same "a compatibility.json-only patch still needs its own rebuild+republish" lesson from the auth-optional-forms task).
  - Command to try against any already-generated project (any layout, with or without `auth`/`rbac` present): `npx nx g @blueprint-platform/modules:user-management --routePrefix=admin/users` — or omit `--routePrefix` entirely for the same default.
- ⬜ Templates pillar — generator logic not started. Components pillar has the `ui` generator only; bigger composite generators (data-table, audit-log) not started. Modules pillar now has `auth`, `rbac`, `user-management` — other business domains not started.
- ⬜ The actual `create-blueprint-app` CLI — not started (currently just a design sketch from earlier discussion).
- ⬜ Automated sync tooling between `blueprint-reference` and `blueprint-platform` — deliberately deferred; syncing is currently manual, file by file.

## Repository layout (this repo)

Absolute path on this machine: `D:\blueprint-platform` (sibling `blueprint-reference` repo is `D:\blueprint-reference`).

```
nx.json                        — release config (independent versioning), plugin config
tsconfig.base.json             — module: nodenext, customConditions: @blueprint-platform/source
package.json                   — root, private, workspaces: ["packages/*"], Nx 23.1.1
packages/
  foundation/                  — ✅ the only pillar with real generator logic
    package.json               — version, publish config, nx.targets.build (tsc + assets globs)
    generators.json            — registers `preset` (factory → dist/.../preset) and `layout` (factory → dist/.../layout)
    compatibility.json          — { foundation, components, modules, templates } version snapshot (NOTE: its own `foundation` field is stale/unmaintained — the devDependency written into a generated project's package.json reads foundation's own package.json version directly instead, not this file)
    tsconfig.lib.json          — excludes src/generators/**/files/** from compilation
    src/
      index.ts                 — (empty) package entry
      generators/
        shared/load-ui-generator.ts — loadUiGenerator(): the components:ui dynamic-require, shared by preset.ts (starter set) and layout.ts (a shell's own UI deps)
        layout/
          layout.ts             — standalone `foundation:layout` generator (see the generator-kit status bullet above for full behavior)
          schema.json/.d.ts     — just `{ name: string }`; validity is a runtime catalog check against files/layout/, not a schema enum (matches components:ui's design)
        preset/
          preset.ts              — the generator entry (default export)
          schema.json            — generator options (source of truth)
          schema.d.ts            — TS type for options (in sync with schema.json)
          preset.spec.ts
          lib/                   — generator helper code, COMPILED into dist
            patch-app-config.ts          — now built on generator-kit's appendProvider; still owns project.json styles array directly
            patch-app-component.ts       — ts-morph swap of NxWelcome for <app-welcome/> (none) or <router-outlet/> (layout) in app.ts/app.html; deletes nx-welcome.ts
            patch-project-environments.ts — updateJson of project.json: dev/test/stg/prd build+serve configs (fileReplacements, no `extends`) + serve.options.proxyConfig
            patch-translate.ts           — ts-morph add provideTranslateService/HttpLoader to app.config.ts + `src/assets` build-asset (showLanguageSwitcher only)
            patch-user-menu.ts           — ts-morph strip <app-language-switcher> wiring from copied user-menu.ts; also called from layout.ts now
            language-registry.ts         — CANONICAL code→{label,dir} map; validates --languages, rendered verbatim into the generated project
          assets/                — binary files, `Buffer`-copied in preset.ts (never generateFiles — EJS-parsing binary bytes risks corruption). favicon.ico, branding/logo.png (→ public/favicon.ico, public/branding/logo.png, unconditional)
          files/                 — templates COPIED verbatim (never compiled). NO shared/ui/ — that moved to components.
          app/app.routes.ts.template
          config/provide-blueprint.ts.template, template-config.ts.template   (EJS <%= %>)
          welcome/welcome.ts   (EJS <%= name %>; → src/app/welcome.ts — themed starter screen, replaces @nx/angular's nx-welcome)
          environments/environment.ts.template (EJS apiUrl/appVersion) + environment.{dev,test,stg,prd}.ts + api-url.token.ts   (→ src/environments/)
          core-api/api.interceptor.ts   (→ src/app/core/ — functional HttpInterceptorFn, prefixes API_URL)
          i18n/en.json, i18n/ar.json    (→ src/assets/i18n/<code>.json, dynamic per-language loop; other codes get en + a `_note` key)
          root/orval.config.ts.template, root/proxy.config.json.template   (→ project root)
          layout/<sidebar|floating|inset|topbar>-shell/…, layout/sidebar-item-flyout.ts (shared), layout/auth-split/, layout/auth-centered/ (content-only shells for modules:auth — see its status bullet)
          theme/theme.service.ts (→ core/theme/, layout only) ; language/language.service.ts (BASE variant, → core/language/, layout OR showLanguageSwitcher) + language.service.translate.ts (augmented variant, written over the base one when showLanguageSwitcher; + rendered language-registry.ts + available-languages.ts)
          styles/theme.scss, tailwind-theme.css, _utilities.scss (hand-authored global-class registry, `@use`d by theme.scss)
          styles/tokens/_emit.scss (mixin), _scale.scss, _palette-default.scss, _palette-brand-x.scss
  components/                   — ✅ `ui` generator + all ~64 spartan-derived UI component sources
    generators.json            — registers the `ui` generator (factory → dist/generators/ui/ui)
    src/generators/ui/
      ui.ts, schema.json, schema.d.ts, ui.spec.ts
      lib/resolve-deps.ts      — in-kit dependency closure (regex) + FLAT_COMPONENTS
      lib/npm-deps.ts          — import-scan → { package: range } version map
      files/shared/ui/<~64 components>/…   (real .ts spartan-derived source, the whole kit)
  generator-kit/                — ✅ shared generator-authoring helpers, NOT a pillar (see the table above)
    src/lib/append-provider.ts, wrap-routes-under-layout.ts, register-ui-path.ts, append-to-manifest.ts, add-layout-branch.ts, append-child-routes.ts (+ one *.spec.ts each)
    src/index.ts               — re-exports all six (add-layout-branch also exports readProtectedRouteBranches, renderChild + the LayoutBranchChild type)
  modules/                     — ✅ `auth`, `rbac`, `user-management` generators (see their status bullets above); other business-domain generators not yet built
    package.json               — deps: @blueprint-platform/foundation, @blueprint-platform/components, @blueprint-platform/generator-kit (generator-package deps, never injected into a generated project — `rbac.ts` itself only imports generator-kit/components, never foundation, even though the shared package.json still lists it for auth's sake)
    generators.json            — registers `auth` (factory → dist/generators/auth/auth) and `rbac` (factory → dist/generators/rbac/rbac)
    src/generators/
      shared/load-ui-generator.ts, load-layout-generator.ts, scan-ui-imports.ts   — shared across auth + rbac + user-management (scan-ui-imports moved out of auth/lib/ once rbac needed the identical pattern; user-management reuses it unchanged)
      auth/
        auth.ts, schema.json, schema.d.ts, auth.spec.ts
        lib/patch-global-styles.ts   — appends the two general-purpose hlm-input-group/autofill fixes (idempotent)
        files/core/auth/…            — models, mock backend, both jwt-*/session-* pairs (unselected one deleted at generation time), storage seam, provide-auth.ts
        files/features/auth/forms/…  — password-match.validator.ts + 6 forms; only login-form.ts.template is EJS-conditional on authType
      rbac/
        rbac.ts, schema.json, schema.d.ts, rbac.spec.ts
        files/core/rbac/…            — models, mock data/users, permission-graph, permissions/roles services (RbacStorage-backed), permission.guard.ts, current-user-permissions.token.ts, rbac-demo-session.service.ts (no .template files — routePrefix only affects routing, never file content)
        files/features/rbac/…        — forbidden/, permissions/{permissions-list,permission-form}/, roles/{roles-list,role-form}/ — NOT preview/ (never synced), NOT rbac.routes.ts (route tree built programmatically via appendChildRoutes)
      user-management/
        user-management.ts, schema.json, schema.d.ts, user-management.spec.ts
        files/core/user-management/…   — models, user-mock.data.ts, role-options.data.ts (static ROLE_OPTIONS, unrelated to RBAC's Role), user-id.ts, user-storage.ts (independent localStorage wrapper — NOT a reuse of rbac's RbacStorage), user.service.ts
        files/features/user-management/…  — users-list/, add-user/, edit-user/, user-form/ (shared add+edit field markup, mode-conditional), details-user/, change-role-dialog/, deactivate-confirm/ (the last three are dialogs, not routes) — NOT preview/, NOT user-management.routes.ts (route tree built programmatically via appendChildRoutes, same as rbac)
  templates/                    — ⬜ bare scaffold only, no generators yet
```

- `preset/lib/**` (and `generators/ui/lib/**`) = generator's own logic → compiled by `@nx/js:tsc` into `dist/`.
- `preset/files/**` (and `generators/ui/files/**`, `generators/auth/files/**`) = payload copied into the generated project → excluded from `tsconfig.lib.json`, shipped via the `assets` globs in each pillar's `package.json` (`**/!(*.ts)` from `src`, plus explicit `**/*.ts` globs — foundation: `files/layout`, `files/theme`, `files/language`, `files/welcome`, `files/environments`, `files/core-api`; components: `files/shared/ui`; modules: `files/core`, `files/features` — plus each generator's `schema.json`, plus foundation's `compatibility.json`). `.ts.template` files (e.g. `environment.ts.template`, `orval.config.ts.template`, `login-form.ts.template`) ride the blanket `**/!(*.ts)` rule since they don't end in `.ts`. Any new `files/` subtree carrying real `.ts` source needs its own `**/*.ts` assets glob — the blanket `**/!(*.ts)` rule deliberately skips `.ts`.
- Theme token system: `_palette-*.scss` files are Sass maps; `tokens/_emit.scss`'s `emit-vars` mixin turns a map into `--var: value` declarations under a selector; `theme.scss` `@use`s the maps and emits the default palette on `:root` and `brand-x` on `.theme-brand-x`. `tailwind-theme.css`'s `@theme inline` only *references* those vars. The two style files never `@use`/`@import` each other.

## Critical architecture decisions (don't relitigate these without reason)

- **Standalone + zoneless + signals only.** `provideZonelessChangeDetection()`, no Zone.js, no NgModules anywhere.
- **CDK + spartan/ui's headless "brain" layer, not a fully pre-styled UI kit.** PrimeNG is explicitly avoided — it went commercial-license-only for government/public-sector use in mid-2026, which matters given some of this developer's client work.
- **Spartan's own CSS variable naming convention is used directly** (`--primary`, `--primary-foreground`, `--background`, `--destructive`, etc. — see `spartan.ng/documentation/theming`), NOT a custom `--color-*` scheme. This means restyling a Helm component to match a new brand palette requires **zero template edits** — only redefining the variable *values* in `theme.scss`. Never reintroduce a custom naming scheme for colors.
- **Tailwind v4 is kept available in generated projects, alongside SCSS**, specifically so consuming developers can use Tailwind utilities for their own custom work outside generated components. Tailwind's `@theme inline` block in `tailwind-theme.css` maps its utility classes to the same CSS variables the SCSS token system defines — one source of truth, two consumption paths. **Tailwind and SCSS files can never `@use`/`@import` each other** — they must stay as separate entries in the `project.json` `build.options.styles` array, in this order: `src/styles/theme.scss` first, `src/styles/tailwind-theme.css` second (Tailwind's `@theme inline` only *references* variables, it doesn't define them), then `src/styles.scss` last (Nx's default global stylesheet, kept so app-level overrides win). `patch-app-config.ts` writes exactly these three. Global hand-authored helper classes (e.g. `.bp-field--floating`) go in `src/styles/_utilities.scss`, which `theme.scss` `@use`s — that partial is the "global class registry".
- **Copy-based generation, not npm-dependency-based, for anything a developer should be able to freely modify.** `@angular/cdk`, `@spartan-ng/brain`, `class-variance-authority`, `clsx`, `tailwind-merge`, `@ng-icons/core`, `@ng-icons/lucide`, `embla-carousel`, `embla-carousel-angular`, `ngx-scrollbar` are real pinned npm dependencies (never copied, never modified) — the last five back specific pieces of the synced UI kit / layout shells. Everything under `shared/ui/`, `shared/components/`, `features/`, `layout/` is copied source the developer owns outright.
- **Flattened/standalone project layout** — generated projects use `rootProject: true` (app lives at the workspace root, no `apps/<name>/` nesting), matching what a plain `ng new` user expects, not Nx's default multi-project nesting.

## Generated project folder structure (src/app/)

```
core/          — app-wide singletons: interceptors, guards, config provider
shared/
  ui/            — foundation pillar: spartan-derived base pieces (button, utils, ...)
  components/       — components pillar: bigger composite pieces, on-demand
features/               — modules pillar + developer's own business domains.
                           Rule: features never import from other features directly,
                           only from shared/, core/, global/.
layout/                       — templates pillar: shells (admin-shell, analytics-shell)
global/                          — cross-cutting concerns no single feature owns
                                    (search, notifications). Empty by default; some
                                    future template generators may populate this.
```
Each feature owns its own lazy-loaded routes file; `app.routes.ts` (foundation-generated skeleton) gets AST-patched by later generators to wire them in.

## Generator conventions

- File is named `preset.ts` (or `<generator-name>.ts`), **not** `generator.ts` — matches what `@nx/plugin:generator` actually scaffolds.
- `app.config.ts` in the generated project is edited with `ts-morph` AST manipulation (`preset/lib/patch-app-config.ts`) — never plain string replacement, since exact file shape shifts across Angular/Nx versions. Structured JSON files (`tsconfig.json` `paths`, `project.json` `targets.build.options.styles`, generated `package.json` deps) are edited with `@nx/devkit`'s `updateJson`, not string replacement either.
- **Routing composition lives in its own helper (`generator-kit`'s `wrapRoutesUnderLayout`), separate from `patch-app-config.ts`** — providers/imports vs. route-tree shape are distinct concerns. It also uses `ts-morph` (wrap `routes[]` as `children` of a lazy-loaded shell route, with an optional `indexRoute` child), never string replacement. A shell component's exported class name is **read from its own copied source** (`export class …` match), not derived from the `layout` value — same principle as computing UI paths/deps from what's actually in the `files/` tree.
- **`layout` enum values carry the `-shell` suffix** (`sidebar-shell`, not `sidebar`) and equal the folder name under `files/layout/` and in the generated `src/app/layout/`; the shell entry file is `<layout>/<layout>.ts`. Don't re-append `-shell` in path construction.
- **`project.json`, not `angular.json`**, holds the `styles` array in Nx-generated Angular apps — there is no workspace-wide `angular.json` in this setup. (This was a real bug hit and fixed mid-project — don't reintroduce the `angular.json` assumption.)
- `compatibility.json` is read via `readFileSync`/`JSON.parse` at generator runtime, **not** via a static `import * as compatibility from '...json'` — the static-import form produced inconsistent interop behavior (values resolving to `undefined`) under this workspace's `module: nodenext` config.
- Any folder under a generator's `files/` directory containing real `.ts` source (meant to be copied verbatim, not compiled) must be excluded from `tsconfig.lib.json`'s compilation (`"exclude": ["src/generators/**/files/**/*"]`) and instead copied via an explicit `assets` glob rule in the package's `nx.targets.build.options.assets` array. Forgetting either half of this causes either TS compile errors (trying to resolve spartan/cva/clsx imports that aren't foundation's own dependencies) or missing files in `dist`.
- **Binary payload (images, icons — anything that isn't text) lives under a generator's `assets/` folder, never `files/`, and is copied with a direct `readFileSync`/`Tree.write` `Buffer`, never `generateFiles`.** `generateFiles` parses every file it copies as an EJS template; running that parse over arbitrary binary bytes risks corrupting them or mis-re-encoding them as text. `assets/` needs its own explicit glob rule too (e.g. `{ glob: "**/*.{ico,png}", ... }`) — verify with a real build that the bytes survive intact (checksum source vs. `dist`, not just "a file with the right name exists").
- **Never let a manually-created `project.json` coexist with `package.json`-based inferred Nx configuration for the same pillar package.** This caused a real, hard-to-diagnose bug: a stale `project.json` created early in the project silently overrode every subsequent edit to `package.json`'s `nx.targets.build.options.assets`, for a long stretch of debugging. If a pillar ever behaves as if config edits aren't taking effect, check for a stray `project.json` first.
- TypeScript path entries (`compilerOptions.paths`) for UI components are **generated programmatically** — the `@blueprint-platform/components:ui` generator writes one `@blueprint-platform/ui/<name>` entry per component it copies (except the flat family). Never hardcode a per-component path list. `preset.ts` no longer touches UI paths at all; it delegates the whole starter-set + layout-set copy (files, paths, npm deps) to that generator.
- **UI-component npm deps are computed, not hardcoded in `preset.ts`.** `ui`'s `lib/npm-deps.ts` scans each copied component's real `from '…'` specifiers and adds only the matching packages. `preset.ts` keeps just `@angular/cdk` + the Tailwind toolchain. Adding a component that needs a new package means updating the `UI_NPM_DEPS` map + `SPECIFIER_TO_PKG` in `lib/npm-deps.ts`, nowhere else.
- **`apiUrl` / `environment.apiUrl` is an origin ONLY — never a path.** `preset.ts`'s `toOrigin()` strips any path/trailing slash before it reaches `environment.ts` or `proxy.config.json`. Reason: Orval generates service methods whose paths already start with `/api/...` (from the backend's own route templates), and `api.interceptor.ts` prefixes `API_URL` onto every root-relative request — so `apiUrl` ending in `/api` yields `/api/api/...`. Do not "fix" this by putting the path back; if a specific backend's OpenAPI paths genuinely lack the `/api` prefix, that's a per-project interceptor/proxy adjustment, not a change to the origin-only rule.
- **Orval config reads the plain `environment` module, not the `API_URL` token.** `orval.config.ts` runs in Node before any Angular app exists — an `InjectionToken` is meaningless there. Both read the same `src/environments/environment.ts` value, which is the actual single-source-of-truth guarantee.
- **Text direction follows the language registry, never a per-code conditional.** `lib/language-registry.ts` (`code → { label, dir }`) is the compile-time canonical source; `preset.ts` validates `--languages` against it and *renders* the generated project's identical `core/language/language-registry.ts` from it (so they can't drift). Adding a language = one registry entry. Never write `if (code === 'ar')` anywhere — `LANGUAGE_REGISTRY[code].dir` is the answer for every language, present and future.
- **Placeholder i18n files.** Only `files/i18n/en.json` + `ar.json` are real. Any other requested `--languages` code is generated as English content with a leading `"_note"` JSON key flagging it needs real translation (JSON has no comments — a key is the substitute). `en`/`ar` never get the `_note` key.
- **Split gating for optional subsystems.** The translation *engine* (service, registry, i18n assets, providers, deps, the `Directionality` alias) is gated on `showLanguageSwitcher` alone — independent of `layout`. The pre-built `language-switcher` UI needs both `showLanguageSwitcher` and a `layout` to live in — but note as of `foundation:layout` (below), the `layout` generator itself has no `showLanguageSwitcher` option; it infers the switcher decision from whether `core/language/` already exists in the target tree. `patch-user-menu.ts` reconciles `user-menu`'s hard import of `LanguageSwitcher` when the switcher isn't generated.
- **Generator-authoring logic shared across pillars belongs in `@blueprint-platform/generator-kit`, not copy-pasted.** Before writing another "read a file, ts-morph it, write it back" or "read/ensure-array/push/write JSON" helper, check whether `appendProvider` / `wrapRoutesUnderLayout` / `registerUiPaths` / `appendToManifest` already covers it — and if the pattern you're generalizing has *moved* since a task description was written (e.g. UI tsconfig-path writing moved from `foundation` into `components:ui` in an earlier task), generalize it where it actually lives now, not where an older doc assumed it still was.
- **A standalone on-demand generator that mirrors a `preset.ts` generation-time option must not need options `preset.ts` didn't pass it.** `foundation:layout` only takes `{ name }` — it recovers the rest (e.g. whether to include `language-switcher`) by inspecting the target tree's actual current state, the same discipline `patch-user-menu.ts` already used. This is also why `preset.ts` calling `layoutGenerator(tree, { name: layout })` reproduces its old inline behavior exactly: by the time that call happens, every earlier preset step it depends on (the translation engine, if any) has already run.
- **Never let a generator delete its own package from a generated project's `package.json` if any of its *other* generators are meant to be runnable against that project later.** `preset.ts` used to delete `@blueprint-platform/foundation` outright; that silently broke `foundation:layout` before it existed to expose the bug. It's now kept as a devDependency (version read from foundation's own `package.json`, not `compatibility.json`'s stale self-entry), same as `components`.
- **Calling another pillar's generator function in-process (not just its templates) needs an explicit deep `exports` entry on the target package** — e.g. `foundation`'s `"./generators/layout"` (mirroring `components`'s `"./generators/ui"`), each with an `@blueprint-platform/source` condition (dev/test), `types`, `require`, and `default`. Node's `exports` field is a strict allowlist once present; `require('@scope/pkg/dist/generators/x/x')` from another published package is rejected without one. The calling package gets a small local shim in its own `generators/shared/` (`loadXGenerator()`, try/catch `require(...)`) — same shape every time, don't inline the `require` at the call site.
- **A generator that composes into another pillar's generator, adding a *sibling* route branch rather than the main app shell, must register that branch as protected** (`generator-kit`'s `addLayoutBranch`, which writes `.blueprint/manifest.json`'s `protectedRouteBranches`) instead of using `wrapRoutesUnderLayout` for its own routing. `wrapRoutesUnderLayout` sweeps every existing top-level route into a new main shell's children — correct for the one main shell, wrong for a branch (e.g. `modules:auth`'s `auth`) that must stay a sibling regardless of when a main shell is added. This is also why reusing `foundation:layout` wholesale for a content-only shell is wrong without its `wireRoutes: false` / `withAccountMenu: false` options — those exist specifically so a shell like `auth-split`/`auth-centered` doesn't accidentally become the app's root wrapper or grow an account menu for a page with no signed-in user.
- **Three routing helpers in `generator-kit`, each for a genuinely different situation — picking the wrong one is a real bug, not a style choice:** `wrapRoutesUnderLayout` wraps *every existing top-level route* under a *brand-new* main shell (used once, when a layout is first added). `addLayoutBranch` adds a *new top-level sibling* that must stay outside any shell forever, and registers it in `protectedRouteBranches` so `wrapRoutesUnderLayout` knows to exclude it later (`modules:auth`'s `auth` branch). `appendChildRoutes` adds *ordinary feature routes that belong inside whatever main shell already exists* — neither a new shell nor a standalone branch (`modules:rbac`'s admin pages) — targeting the one top-level route that has its own `children` AND isn't in `protectedRouteBranches`, or the flat top-level array if there's no shell at all. All three take/return route-tree shapes built from the same recursive `LayoutBranchChild` (path, optional `redirectTo`/`pathMatch`, optional `componentImportPath`+`componentClassName` leaf, optional nested `children`) — a real nested tree (not single path strings with slashes) matters whenever the copied components navigate relatively (`router.navigate(['..'], { relativeTo })`), since that depends on route-CONFIG nesting depth, not the URL string.

## Registry / publishing conventions

- Real registry: **npm public registry**, scope `@blueprint-platform` (chosen because `@blueprint` itself may not have been available as an npm org — confirm before assuming).
- Local testing registry: Verdaccio (`npx verdaccio`, `http://localhost:4873`).
- **Critical gotcha:** if `.npmrc` (project-local or global `~/.npmrc`) has a scope-specific registry mapping (`@blueprint-platform:registry=...`), it silently overrides any `--registry=` flag passed to `npm publish`/`npm dist-tag`/`create-nx-workspace` for that scope. Use the scoped-override flag form instead when testing locally: `npm publish --@blueprint-platform:registry=http://localhost:4873`. Remember to point the scope back at the real npm registry once local testing is done.
- Two dist-tags drive version resolution: `latest` (bleeding edge) and `stable` (tested-as-a-set, gated by manually updating `compatibility.json`). The CLI (once built) should resolve `stable` by default, never resolve an embedded version string directly in `--preset=@scope/pkg@x.y.z` — this is unreliable on current Nx for scoped packages (known upstream bug, Nx issue #23174) and should always go through a dist-tag instead.
- `package.json` for a publishable pillar needs: `"generators": "./generators.json"` (top-level field, easy to forget), an `exports` entry for `./generators.json` specifically (Node's `exports` is a strict allowlist), and a `"files"` array including `dist`, `generators.json`, and `compatibility.json` (otherwise `.gitignore`-based default packing may silently exclude `dist/`).

## Workflow discipline (from the governance doc, still applies)

- `blueprint-reference` folder structure mirrors the pillar targets 1:1 (`src/app/ui/button/` ↔ `packages/components/src/generators/ui/files/shared/ui/button/` — UI kit now lives in the components pillar, not foundation) specifically so syncing is mechanical, not a judgment call.
- A piece is "sync-ready" only when: it uses only spartan/theme CSS variables (no hardcoded colors), has no console errors, works in both RTL and LTR if layout-sensitive, and the brain/behavior layer is provably untouched from spartan's original.
- Every sync gets a `SYNC_LOG.md` entry and a git tag (`sync/<name>/<version>`) in `blueprint-reference`, recording exactly which reference-app commit backs which published pillar version.

## When picking up work in this repo

1. Check `compatibility.json` and each pillar's `package.json` version to know current published state.
2. Check this file's "Current status" section (keep it updated) before assuming what's built.
3. Any new generator template content should first exist, verified, in `blueprint-reference` — unless it's genuinely generator-only content with no reference-app equivalent.
4. Test locally against Verdaccio before ever publishing to real npm.
