# cli-core

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build cli-core` to build the library.

## Running unit tests

Run `nx test cli-core` to execute the unit tests via [Jest](https://jestjs.io).
