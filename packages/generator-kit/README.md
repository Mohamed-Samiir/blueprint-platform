# generator-kit

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build generator-kit` to build the library.

## Running unit tests

Run `nx test generator-kit` to execute the unit tests via [Jest](https://jestjs.io).
